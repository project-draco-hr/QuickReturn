{
  super(context,0,googlePlusPosts);
  mContext=context;
  mGooglePlusPosts=googlePlusPosts;
  mInflater=LayoutInflater.from(mContext);
  mTransformation=new RoundedTransformationBuilder().cornerRadius(QuickReturnUtils.dp2px(getContext(),50)).build();
}
